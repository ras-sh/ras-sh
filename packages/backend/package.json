{
  "name": "@ras-sh/backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "cli": "npx tsx --env-file=.env.local src/cli.ts"
  },
  "dependencies": {
    "@erquhart/convex-oss-stats": "catalog:",
    "@ras-sh/convex-cli": "workspace:*",
    "convex": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
